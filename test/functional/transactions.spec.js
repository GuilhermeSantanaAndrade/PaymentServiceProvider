const chai = require("chai");
const assert = chai.assert;

describe("Autenticação", () => {
  it("Devemos autenticar com credenciais válidas e o resultado deve ser uma API Key", () => {
    assert.isOk(true);
  });
  it("Devemos autenticar com credenciais inválidas e o resultado deve ser status 401", () => {
    assert.isOk(true);
  });
});
